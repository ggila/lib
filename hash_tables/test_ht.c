#include <time.h>

void	test_insertion(int size, int nb_insert, int seed)
{
	t_hash_tbl	ht;
	int			i;

	ht_init(&ht, size, int_cmp, int_hash);
	i = 0;
	while (i < size)
	{
		ht_put()
	}
}

int		main(int ac, char **av)
{
	int seed;

	seed = time(NULL);
//	test_insertion();
}
